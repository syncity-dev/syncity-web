# Revisions to skip in `git blame` — pure formatting, no behaviour change.
#
# GitHub reads this file automatically. Locally, enable it once with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: format src with prettier
b1c622d7600fa184ea892137b83508b056a7804c
